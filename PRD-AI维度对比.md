# PRD · AI维度对比（小红书）（0→1）

| 项 | 内容 |
|----|------|
| 作者 | 用户（AI PM） |
| 日期 | 2026-09-21 |
| 状态 | 全模块落地，待终稿综合 |
| 产品类型 | AI / RAG 产品（站内功能） |
| 关联看板 | `board-A-memory.md` · `board-B-status.md` |

> 本 PRD 由分模块增量构建而成；各章节的事实依据见看板 A，构建状态见看板 B。

---

## 1. 产品目标与范围（Why / How / What + 目标用户与场景）

### 1.1 Why（为什么做）

> 论证结构：**现状是什么 → 卡点是什么 → 做的前提条件（数据基础 / 技术条件）→ 做了的收益（用户 / 平台）**。

#### 1.1.1 现状是什么

**论点：旅程上游（发现内容 + 发现SKU）已被站内解决；"对比"这一步靠用户手动凑合，全行业无人供给中立对比层。**

**用户旅程现状**（以帐篷选购为例，对照点点现状，F33/F34）：

| 阶段 | 用户行为 | 现有承接 | 结果 |
|------|----------|----------|------|
| ① 种草期 | 刷到多篇帐篷笔记，各说各话（A讲速开、B讲轻量、C讲空间），收藏5-10篇 | 内容分发 | 无判断依据，收藏堆积 |
| ② 泛搜发现SKU | 搜"帐篷怎么选"→ 点点出AI攻略（参考56篇内容）+ **给出几个SKU**（牧高笛零动155/挪客ango/迪卡侬Quechua等超链接）；攻略结尾按预算区间给通用"怎么选"准则 | **点点泛搜（内容层已解决）** | 内容56→1，SKU候选已浮出 |
| ③ 对比缺失 | SKU到手但没有对比层：攻略线性、SKU列表陈列，SKU间维度差异不可见，仍需逐个点开商品页/笔记拼交集；"170+站着舒服"等适配信号散落正文 | **无（本功能切入点）** | 交集仍靠用户脑内拼装 |
| ④ 决策悬置 | 读完没有下一步收尾，回到收藏夹反复横跳，或截图问朋友 | 无 | 纠结延续，决策疲劳 |
| ⑤ 验证外流 | 跳淘宝/京东查价看差评，顺手站外下单 | 种草直达 | 成交外流 |

**补充现状事实**：

| 侧面 | 现状 |
|------|------|
| 站内成交 | 站内转化率仅 0.7-1.2%，98% 的种草成交发生在站外——用户把小红书当"决策内容源"，不在站内走完决策 |
| 用户凑合行为 | 对比这一步今天靠：手动开多篇笔记取交集（成本高）/ 截图问朋友（样本偏差）/ 跳出去问豆包（无人群适配的通用答案） |
| AI 竞品格局 | 千问+淘宝、豆包+抖音、Rufus 全是促交易立场的导购 AI——"种草之后、下单之前"的中立对比层无人供给；对话问完即遗忘，无可转发、可翻案的决策物件 |

#### 1.1.2 卡点是什么

**论点：4 个交易卡点全部落在"SKU 得到之后"（旅程③④）——恰恰是最接近成交的环节；上游的泛搜与 SKU 发现已被点点解决（F34）。**

**用户侧卡点**：

| # | 卡点 | 现状证据 | 对应方案 |
|---|------|----------|----------|
| 1 | **阅读成本**：内容已聚合（56→1）但仍为线性文章，SKU间差异需通读拼装 | "不够精简"（用户原话）；按商品分节讲完而非按维度拉齐 | 用户选题+维度行卡片 |
| 2 | **SKU陈列无对比结构**：泛搜给出SKU超链接，但SKU间维度差异不可见，用户逐个点开自行拼装 | 攻略讲"迪卡侬Quechua系列"特点，不与零动155按维度拉齐 | 用户组品（F28）+维度行对比 |
| 3 | **无个性化裁决**："怎么选"=通用if-else准则（人数/交通/预算），人群适配（身高/肤质/新手）不进入结论 | "170+站着舒服"散落正文，155cm用户易漏看 | 分群结论（F19） |
| 4 | **无决策终局**：读完没有"买/不买/再看看"，转化漏斗无收口 | 攻略结尾是通用准则，无收尾交互 | 三选一收尾卡（F12） |

**功能边界与分工**（F34）：点点=泛搜→SKU发现（内容层聚合，解决"找内容+找到候选"成本，已上线）；本功能=**得到SKU后针对SKU对比**（决策层结构化，解决"读内容+做裁决"成本）。点点基建恰是上游（F22），入口即点点泛搜结果的CTA——站内演进攻防而非重复造轮子。

**平台侧卡点**（"不做"正在支付的代价）：
1. **高意向流量滞留文章层**：已锁定SKU、正在对比的用户是最接近成交的流量，被留在"线性攻略+决策悬置"而非交易层
2. **信任损耗延迟支付**：虚假种草的成本不在种草时爆发，而在收货/售后阶段延迟支付——女装退货率 50-60%（货不对板为主因）、避雷笔记反噬内容生态
3. **决策入口正在被外部 AI 接管**：千问/豆包正在养成用户"种草后去 AI 问"的习惯——不承接，小红书将从决策入口降级为内容供给方，比 GMV 外流更深一层

本功能把旅程③④从「文章+悬置」改写为「结构化证据+站内终局」。

#### 1.1.3 做的前提条件（为什么能做、为什么只有站内能做）

**论点：数据站内独有（认证的信任）、技术零新基建（上游现成+管线已验证）、渠道零新开（寄生既有动线）——不是"能不能做"的问题，是"只有这里能做成"的问题。**

**数据基础——站内独有，构成"认证的信任"壁垒（第三方产品只有"推测的信任"，F11）**：

| 独有数据 | 对比管线的作用 | 第三方为何拿不到 |
|----------|----------------|------------------|
| 笔记语料（视频ASR带时间戳/图文OCR可机读） | 真实维度（淋雨实测/155偏大/溶解度）只在笔记里——淘宝参数表没有这些维度，豆包没有这个密度的语料 | 小红书自家内容仓库 |
| 已购标记（订单级 user×sku） | 「已购实测」加权——评价污染的第一道防御 | 只能靠推断"谁真买过" |
| 报备商单库（蒲公英） | 确切知道哪篇是广告（非推断）→ 只降权、不删除、不标"广告" | 站外只能猜测软广 |
| 未挂载笔记的检索匹配 | 真实维度大量散落在未挂载笔记，检索归入对比池——不依赖挂载率 | 站外拿不到内容 |

外加 SKU 结构化数据（客观层直读、不经 LLM）；数据全景与跨数据集口径见 §4.5。

**技术条件——零新基建，可行性已验证**：
- **上游现成**：点点意图识别与检索基建已上线（F22）——不是造新引擎，是把既有检索按"商品×维度词"复用
- **管线已验证**：抽取/汇编按 spec-driven SDD 真实落地，护栏（幻觉丢弃/导购词拒收/样本不足标注/报备降权）全部为可测确定性规则，24 条用例全绿（§4.2 落地状态）；mock 适配器与真实接口同签名，接真实数据只换适配器、零改业务代码
- **知识表是自建轻资产**：3-9 维/类目人工维护，冷启动两套已建；自定义维度回写 candidate→人工审核→preset，资产随使用增厚

**渠道条件——零新开流量成本**：DAU 1.2 亿原生入口，寄生既有动线（泛搜现场勾选 / 市集☆ / 商品页加入购物车 / 购物车抽屉勾选开比），不新增一级入口。

**生态条件——商业上站得住的先例**：淘宝"问大家"+差评置顶没有杀死直通车——本功能锚定商品质量、永不锚定笔记是否广告（F10），只砸劣质供给、不砸内容生态。

#### 1.1.4 做了的收益（用户 / 平台）

**论点：用户侧 4 个卡点一一偿还；平台侧四本账，其中防御账最深。**

**用户收益——4 个卡点一一偿还**：

| 卡点 | 用户得到什么 |
|------|--------------|
| 阅读成本 | AI 替你翻 N 篇笔记按维度拉证据——56 篇的阅读量收敛为一张表 |
| SKU陈列无对比结构 | 非标品横向比首次可行：参数表拉不开差距的品类，真实维度（实测/人群适配）结构化拉齐 |
| 无个性化裁决 | 分群结论（155/170+/混油皮），适配信号不再散落正文漏看 |
| 无决策终局 | 三选一收尾+对比卡转发问朋友，读完有收口——不再收藏夹反复横跳、决策疲劳 |

**平台收益——四本账**：

| 账 | 收益 |
|----|------|
| 收入账 | 验证环节留站内 → 红猫联盟抽佣+闭环转化；广告卖心动、报告卖确认——预算场景互补不打架 |
| 成本账 | 货不对板在对比阶段被提前暴露 → 退货率下降（女装 50-60%），用户/商家/平台三方节约 |
| 资产账 | 避雷笔记变产品功能；已购证词正循环，语料越用越厚；品类知识表+B端选品简报可复用 |
| 防御账（最关键） | 承接"种草后去 AI 问"的习惯，锁住决策入口不降级为内容供给方；中立对比层是组织利益锁死在促交易的豆包做不到的——裁判不能兼运动员 |

### 1.2 目标用户与使用场景

**核心用户**：小红书高频用户（女性约70%、95后约50%、一二线约50%）中处于**非标品购买决策期**的人——被种草多件同类商品、正在纠结选哪件。

**典型场景**：
1. **泛搜得SKU后仍纠结**：用点点搜"帐篷怎么选"拿到攻略和几个SKU，但SKU间维度差异不可见，还得逐个点开拼交集
2. **收藏夹聚集**：收藏了同品类多件商品，反复来回看、无法决定
3. **评论区求对比**：笔记评论区"求对比A和B"，无人回应

**现状替代方案**：点点AI攻略（内容聚合但线性、类型级、无终局，F33）/ 手动开多篇笔记取交集 / 截图发朋友问 / 跳出去用豆包问（得到无人群适配的通用答案）。

### 1.3 How（怎么解决，方向性）

把用户正在手动做的"开多篇笔记取交集"自动化：AI识别对比需求 → 复用点点检索能力按维度词搜笔记 → 抽取结构化维度元组（带人群标签和证据强度）→ 差异优先的可视化对比 → 决策终局收尾。报告锚定商品质量，永不锚定笔记是否广告（看板A F10）。

### 1.4 What（做什么 / 范围）

**v1 做（最小闭环，F21）**：
- AI维度对比报告：品类×维度词检索（复用点点）、结构化维度抽取（品类知识表脊柱）、差异优先可视化（维度行卡片+人群标签分群+溯源+品类基准线）、三选一收尾（买/不买/再看看）+ "都没那么好"、对比卡转发
- 品类范围：女装+护肤双类目首发（覆盖"即时穿搭"与"长期护肤"两类决策模式），户外/母婴后续复制

**v1 不做（v2+）**：
- 讨论层（价格公投/偏好光谱/证词翻案/后悔指数）
- 单品拔草报告（共用管线但独立产品形态）
- 对比卡互动化（v1为静态转发卡）

---

## 2. 核心工作流与目的（PRD 核心）

**功能名：「比比」**（F39）。可交互demo见 `demo/`：入口 `demo/index.html`——链路A：首页（复刻，点点=**左上角搜索图标**可点，F47）→ 点点泛搜（预填3件/换一换）→ 维度选择（热门预勾+loading态）→ 对比报告（表格/溯源/修改对比对象/＋追加/三选一收尾）；链路B：市集 ☆ / 商品页「加入购物车」收进购物车 → 市集/商品页右缘「比比」竖条或🛒购物车图标（数量角标）唤起购物车抽屉（商品页当前浏览置顶+条目勾选开比，跨类目勾选自动排除）→ 维度选择 → 对比报告（市集商品卡点击仍可进mock商品详情页）。**口径边界（F48）**：收集侧沿用购物车叫法（用户不理解「比比」是什么），进入比较界面（维度选择/对比报告）后才称「比比」。

**总动线**：组品（入口A=点点泛搜结果页勾SKU / 入口B=购物车勾选开比（F46与购物车合并，F48口径回调：收集侧称购物车）——市集 ☆ 与「加入购物车」写入同一购物车，市集/商品页右缘「比比」竖条唤起购物车抽屉内条目勾选确认组品）→ 维度选择页 → 对比报告页（表格）→ 三选一收尾 / 转发分享。三个界面均为站内页面，动线单向无回环；任一步返回上一步，勾选/选择状态保留。

### 2.1 功能&动线

| 功能 | 详情 | 跳转 |
|------|------|------|
| **【入口A·泛搜现场】**<br>SKU卡片多选 | **触发逻辑**：用户在点点搜品类词（如"帐篷怎么选"）后的结果页——攻略文章+SKU卡片列表流；SKU卡片右上角新增圆形多选框（F35）。<br>**多选框显示规则**：<br>1. 默认隐藏，首勾任意一张卡片后，其余卡片多选框同步显示；<br>2. 已选满3件（F29上限）：未选卡片多选框置灰不可勾。<br>**勾选方式**：<br>1. 手动勾选（主路径）：逐张点选2-3件；<br>2. 意图预填（辅助）：query含"对比/vs/怎么选"时，系统预勾高讨论度SKU 3件，浮条文案变「帮你预选了3件，换一换？」，点「换一换」清空预勾转手动。<br>**勾选作用域**：勾选结果在当前泛搜结果页内保留（翻页/加载更多不清空）；从下游页面返回不清空；离开泛搜页或发起新搜索后清空，不跨页持久化。<br>**【demo】**：SKU卡片为mock两组例子（帐篷3 SKU/西装3 SKU）；意图预填按query关键词命中例子组；跨例子组勾选时提示「这两件不是同类商品，先比同类吧」并锁定先选的组。 | 已选≥2件：浮条主按钮可点 → **push 维度选择页**<br>已选<2件：按钮置灰，文案「再勾1件就可以比啦」，不阻断阅读 |
| **【入口A·泛搜现场】**<br>吸底浮条 | **字段布局**（自左向右）：<br>1. 已选SKU缩略图横排（实时更新，最多3个）；<br>2. 文案区：「比一比这N件」/「帮你预选了3件，换一换？」随场景切换；<br>3. 主按钮「比一比这N件 →」。<br>**出现/收起**：首勾后滑入吸底展示；全部取消勾选后滑出消失。<br>**各action触发内容**：<br>1. 主按钮：校验已选≥2件通过 → push维度选择页；<br>2. 「换一换」（仅意图预填态）：清空预勾，转手动勾选。 | 主按钮 → 维度选择页 |
| **【入口A·泛搜现场】**<br>长按换品 | **触发逻辑**：长按已勾SKU卡片 → 半屏弹层列同类候选商品（调换品接口，同品类按相关性排序）。<br>点选候选即原位替换并保持勾选态；点空白处收起弹层。 | 弹层内点选 → 原位替换，留在泛搜结果页 |
| **【入口B·收集】**<br>市集 ☆ / 加入购物车 | **触发逻辑（F46 合并 / F48 口径回调）**：市集商品卡主图右上角 ☆，点亮即**收进购物车**（跨页持久化，不限件数——收集无上限，对比上限在开比时校验），再点亮即移出；点亮toast「已加入购物车，勾选同类即可开比」。商品页底栏「加入购物车」写入同一购物车（toast「已加入购物车，勾选同类即可开比」；已在车中则「购物车里已经有啦」并唤起抽屉）。<br>**定位**：纯收集动作，不弹面板不跳页，留在当前页继续逛；购物车即比比的组品池（F46合并；F48口径回调：收集侧沿用购物车叫法，不引入「比比篮」新词——用户不理解比比是什么）。 | 无跳转（收集态，留在市集页） |
| **【入口B·唤起】**<br>购物车抽屉·勾选开比 | **位置（F47竖条 / F48口径回调）**：①**右缘「比比」竖条**——市集页与商品详情页两页常驻（垂直排版、品牌红渐变、无数量角标），竖条即「比比」的入口；②商品详情页顶栏/底栏 🛒购物车图标（均带数量角标）；③市集页金刚区「购物车」入口。点击均滑出同一侧边栏抽屉（**标题「购物车 · 勾选开比」**——收集侧称购物车，进入比较界面后才称比比）：<br>1. **商品页**：当前浏览商品置顶（带「当前浏览」标；未入车时该行显示「＋ 加入」快捷键）；**市集页**无当前商品，直接列车内条目；<br>2. 车内条目列表：每条**圆形勾选框**（勾选=参与本次对比，默认全勾）+ ✕逐件移出；<br>3. 空车态：引导文案「点『加入购物车』或回市集点亮 ☆，勾选 2 件同类商品即可开比」（市集页版「点亮商品卡右上角 ☆ 收进购物车」）。<br>**CTA**：勾选≥2件激活「比一比勾选的N件」；0件置灰「先勾选想比的商品」，1件置灰「再勾1件就可以比啦」。<br>**补货入口（F49）**：抽屉底部hint区右侧常驻「去点点搜搜 ↗」链接（次级动作色，不与CTA争权重，空车/有货均可见）→ 点点搜索。真实版=落搜索输入页由用户输入想搜什么；**【demo】**：直接落mock泛搜页+到页toast「演示：真实版会先让你输入想搜什么，这里直接展示mock结果」。<br>**跨类目边界（F46）**：勾选跨类目时触发开比 → **自动排除其他类目条目**并toast「已自动排除其他类目N件，比比仅支持同类目对比」；主导类目以当前浏览商品为准（市集页无浏览商品，以首个勾选项为准）；排除后同类<2件 → 拦截并toast「已自动排除其他类目N件，同类目再勾N件才能比」；同类勾选>3件 → 拦截「一次最多比3件，先取消勾选多余商品」（F29）。 | CTA → **push 维度选择页**<br>（校验同类≥2件通过）<br>「去点点搜搜」→ 点点搜索页 |
| **【页面1】维度选择页·布局** | **字段布局**（自上而下）：<br>1. 商品摘要条：已选2-3件缩略图+名称+价格横排；点缩略图回商品详情页；<br>2. 预设维度chips：来自品类知识表，分**客观参数组**（价格/重量/防水…）与**实测主观组**（空间/版型/肤感…）两段；热门3-5个默认预勾（hot_rank，F32）；<br>3. 自定义输入框：占位文案「还想比什么？如'显白'」；<br>4. 底部主按钮「开始对比」。<br>**【demo】**：自定义输入框隐藏（F37）。 | 「开始对比」→ **push 对比报告页**，进入loading态（「正在翻N篇笔记…」，逐维度渐进渲染：先出的维度先显示）<br>返回键回泛搜结果页（勾选保留） |
| **【页面1】chips交互** | 1. 点chip切换勾选/取消；<br>2. 全部取消勾选时「开始对比」置灰，文案「选一个想比的维度吧」；<br>3. 自定义维度输入回车 → chip区顶部追加橙色自定义chip，触发点点按"商品×维度"检索（追加的是证据陈列，不是打分，F26/F30）；语义模糊时反问收窄一轮（§3.3）。**【demo：输入框隐藏，无此路径】** | — |
| **【页面2】商品头卡**<br>（客观条件层） | **位置**：表格首行，固定吸顶、可折叠（F31 客观上/主观下）。<br>**字段布局**（每列一件商品）：商品主图（方形）+ 名称（一行，过长截断）+ 价格 + 核心参数。<br>客观条件层直读SKU结构化参数，不经过LLM（规则层）。<br>点「折叠」收起为一行缩略条，再点展开。<br>**修改对比对象**（F44）：头卡层「✎ 修改对比对象」入口 → 滑出备选栏抽屉（与购物车抽屉同形态）：当前对比区（✕移出，至少保留2件）+ 同类候选区（＋换入，上限3件；**候选按类目过滤，跨类目SKU不出现**）→ 确认后重跑报告（事实标签重算/人群重分群/无数据如实展示）。 | 点主图 → 商品详情页（新页打开，返回回报告页且状态保留）<br>「✎ 修改对比对象」→ 备选栏抽屉 |
| **【页面2】维度行**<br>（表格主体） | **布局**：维度为行、商品为列（F38）；行首为维度名，单元格=该商品该维度的证据陈列。<br>**单元格字段**（自上而下）：<br>1. 结论（忠实转述，14px半粗）；<br>2. 证据数角标（"N条证据"）；<br>3. 高赞证词摘要（13px灰，一句）；<br>4. 人群标签chip（"155-160cm"/"混油皮"…）。<br>**排序规则**：客观行置顶（客观上/主观下，F31）——价格/重量等直读参数行固定在最前；主观行内差异优先——商品间有差异且证据充分的维度排前（行首红色「差」角标，首屏只放拉开差距的维度，F17）；结论一致的维度折叠至表格底部（点开可看）。<br>**边界标注**：某商品某维度证据<3条 → 结论旁灰标「样本不足（N条）」且排序下沉；无证据 → 单元格显示「无数据」，禁用通用知识填充（F30）。<br>**冲突处理**：同一维度结论矛盾 → 按人群标签分群陈列，不求平均不择优（F19）。 | 点单元格结论 → 半屏溯源浮层 |
| **【页面2】添加维度「＋」** | **入口**：表格末行固定「＋ 添加对比维度」按钮（F38）。<br>**正式版**：点「＋」弹输入框 → 输入自定义维度（如"显白"）→ 澄清（至多一轮）→ 检索追加维度行（追加证据陈列，不是打分）。<br>**【demo】**：点「＋」直接填入下一个预设维度行（mock追加：不弹输入框、不真检索，F38）。 | 追加行按差异优先规则插入表格（页面不刷新） |
| **【页面2】溯源浮层** | **触发**：点任意单元格结论 → 半屏浮层。<br>**字段布局**：<br>1. 证据原文片段（完整snippet）；<br>2. 证据强度标签（实测视频/实测图文/开箱/观点陈述，F18）；<br>3. 该维度全部证据列表（按强度+点赞数排序）；<br>4. 底部按钮「查看原笔记」。 | 「查看原笔记」→ 笔记页（视频笔记带时间点参数，直达相关片段）；返回回报告页 |
| **【页面2】收尾卡** | **位置**：报告尾部固定卡（F12）。<br>**字段布局**：<br>1. 三个主按钮：「买」「不买」「再看看」；<br>2. 次按钮：「都没那么好」；<br>3. 「转发」按钮。<br>**交互**：点任一终局按钮 → 按钮高亮+轻提示「已记录你的决定」→ 卡片收起为一行摘要（终局埋点=北极星事件，F12/F15）；点「转发」→ 生成转发卡。<br>**「买」终局后**：卡片下方展示所选商品挂链（闭环/种草直达）——链接展示但不预选中，防导购倾向（F30）。 | 「转发」→ 对比转发卡 |
| **【回流】对比转发卡** | **形态**：静态图卡，不交互。<br>**内容**：3件商品缩略图 + 关键差异维度摘要（取报告前2个差异维度）+ 文案「我也在纠结，帮我看看」。<br>**分享渠道**：系统分享面板（微信/站内私信/收藏）。<br>**回流**：好友点开 → 落地同一对比报告页**只读态**（可看全部证据，不可改维度）→ 好友可自己从入口A重新组品选题。 | 好友点开 → 对比报告页（只读态） |

### 2.2 AI / 规则 / 人工边界

意图识别与检索复用点点（F22/F25/F26）；维度抽取与条件汇编=LLM+品类知识表（AI核心）；客观条件层=结构化参数直读（规则）；可视化渲染=模板（规则）；组品与维度选择=用户手动（F28）。

---

## 3. AI 核心人设（系统提示词 / 对话逻辑 / CoT）

### 3.1 人设定位

双AI角色，均为"无立场"人设（F30：只陈列证据，永不裁决）：

| 角色 | 人设 | 职责 |
|------|------|------|
| **维度抽取器** | 忠实转述者 | 从笔记抽取(维度,结论,人群,证据强度,原文片段)元组；只转述、绝不推断补全 |
| **条件汇编器** | 证据陈列师 | 把多商品元组汇编成对比条目；只陈列、不评价、不推荐、冲突分群展示 |

### 3.2 系统提示词（草案）

**维度抽取器：**
```text
你是小红书笔记的维度抽取器。任务：从给定笔记中，抽取与指定品类知识表维度相关的陈述元组。

铁律：
1. 只抽取笔记明确陈述的内容，禁止推断、外推或用常识补全
2. 每条元组必含：维度｜结论（忠实转述原文语义，不加工不强化）｜人群标签（作者/提及者的可识别特征：身高体重、肤质、使用场景；无法识别标"通用"）｜证据强度（实测视频/实测图文/开箱/观点陈述）｜原文片段（溯源用）
3. 未提及的维度输出"未提及"，禁止填充
4. 保留原文情感强度（"超级显白"保留强度词），不改写为你的语言
5. 不判断笔记是否广告（报备标记由上游数据提供）
```

**条件汇编器：**
```text
你是对比条目汇编器。你收到多件商品的维度元组集合，输出结构化对比条目。

铁律：
1. 只陈列不裁决：禁止"建议选X""X更值得买"等任何推荐性表述
2. 冲突不调和：同一维度结论矛盾时按人群标签分组展示，禁止求平均或择优
3. 样本量透明：每条结论标注证据数；某维度某商品证据<3条时显著标注"样本不足"
4. 差异优先排序：商品间存在差异且证据充分的维度排前；结论一致的维度折叠
5. 每条结论可溯源到笔记ID
6. 无证据的维度留空标"无数据"，禁止用通用知识填充
```

### 3.3 对话逻辑

条件输入式交互（非自由对话），主交互是**用户选题**：
- **维度勾选（主路径）**：用户从预设菜单勾选维度（颜值/耐用性/版型/肤感…）→ 点点按"商品×维度"检索 → 抽取陈列该维度证据行
- **自定义维度（次路径）**：输入菜单外维度（如"显白"）→ 同样触发点点检索追加（F26/F30：追加的是证据陈列，不是打分）
- **澄清（最多一轮）**：自定义维度语义模糊（如"好用吗"）时反问收窄（"你关心持妆、遮瑕还是肤感？"）
- **收束**：三选一收尾即结束，不延伸闲聊、不做追加推荐

### 3.4 CoT / 推理策略

- 抽取器内部CoT：笔记类型识别 → 逐维度扫描 → 原文核对；汇编器内部CoT：维度归集 → 冲突检测 → 分群 → 差异排序
- CoT一律不外显：用户可见层只有结构化条目+溯源入口，保证"证据即界面"

---

## 4. 前端设计 + 后端架构

### 4.1 前端设计（风格）

**风格基调**：完全沿用小红书站内原生设计语言（不自创体系）——白底浅灰分隔、圆角卡片（12px）、系统字体、主按钮品牌红（#FF2442）、chips灰底选中描红。对比表格是新组件：单元格内证据陈列采用左对齐小字排版（结论14px半粗+证词摘要13px灰+证据数角标），差异维度行首加红色「差」角标，折叠行灰显。动画仅三处：吸底条滑入、抽屉（购物车/修改对比对象）滑出、维度行渐进渲染淡入——其余静默，工具感优先于炫技。

界面清单与逐页入口/交互/动线详见§2（SKU选取=泛搜结果页、维度选择页、对比报告页三界面）。

### 4.2 后端架构

**v1冷启动：全mock数据层，无真实搜索**（F36/F37，用户无小红书接口，演示demo）。SKU库、笔记库、点点检索均不做真实实现——**维度选择完成后直接传对应例子的mock笔记进管线**。业务管线（抽取/汇编/渲染）为真实实现。

```
入口层（点点意图识别 [demo：首页左上角搜索图标，query含对比/vs/怎么选→预填] + 市集/商品页右缘「比比」竖条唤起购物车抽屉）
   ↓
组品（[demo：泛搜SKU卡片多选 / 购物车（☆与加入购物车收集+商品页当前浏览置顶+条目勾选，跨类目自动排除）→ mock SKU库两组例子]）
   ↓
维度选择（品类知识表[真实维护] → 预设菜单，热门预勾）
   ↓
检索层（[demo：无真实搜索——初始维度与追加维度均直接取mock笔记]）
   ↓
抽取层（LLM维度抽取器【真实】 → 证据元组）
   ↓
汇编层（LLM条件汇编器【真实】 → 对比条目JSON；差异排序/分群/样本量标注）
   ↓
渲染层（模板引擎【真实】 → 前端组件树） + 数据层（决策终局埋点）
```

关键架构决策：①**demo只验证两件事——抽取质量与交互形态**，SKU发现与检索是点点既有基建，不在验证范围（F34边界）；②mock适配器与真实接口同签名，后续接真接口只换适配器、零改业务代码；③真实上线时检索层按"商品×维度词"调点点搜索API、已购/报备标记由上游附加——该逻辑在demo中以笔记mock字段预演。

> **落地状态（2026-09-22，spec-driven SDD，specs/001-bibi-compare）**：上述管线已在 demo 真实实现并通过测试——`demo/src/data/`（**两类目知识表16维**[帐篷9维+西装7维，均带category字段]/**25篇mock笔记**[帐篷18篇=12初始+6换品候选；西装7篇]/mockLLM查表）+ `demo/src/pipeline/`（extractor 组装 §3.2 系统提示词并注入LLM适配器、assembler 九条确定性规则+面料成分客观值分支、index buildReport/appendDimension **按组首SKU类目过滤知识表，跨类目维度不进报告，F46**），UMD 双端（浏览器 file:// + Node `node --test`，24条用例全绿）；`report.html`/`dimensions.html` 已接线消费管线输出，页内零硬编码维度数据，**换品抽屉候选按类目过滤**。护栏（幻觉丢弃/导购词拒收/样本不足标注/报备降权）均为可测规则并有演示数据（帐篷note_t_011/西装note_s_004）。入口B已与购物车合并（F46）：单一存储（`bb_cart`），市集 ☆、商品页「加入购物车」、抽屉勾选三处共写；**F47（2026-09-22）**：右缘「比比」竖条回归并扩至市集+商品详情页两页，首页点点=左上角搜索图标；**F48（2026-09-22 口径回调）**：F47 曾将购物车口径整体改名比比篮（storage `bb_basket`），同日回调——收集侧恢复购物车叫法（用户拍板：用户不理解「比比」是什么），storage 回 `bb_cart`，「比比」仅用于右缘竖条入口与比较界面（维度选择/对比报告）。
>
> **设计系统（2026-09-21，`demo/DESIGN.md`，Google Stitch 格式）**：前端设计已按 DESIGN.md 方法论完成——令牌层（品牌红四档/墨阶四级/证据语义色三元组）沉淀为 `assets/style.css` 的 CSS 自定义属性，六页全部收编；核心原则「视觉中立」：品牌红只出现在价格/入口/CTA，**结论永远是墨色**，证据强度=冷蓝、人群=中性灰、警示（样本不足/分歧/报备）=琥珀橙——F30 中立立场直接编码进色彩语义。

#### 4.2.1 Mock数据集（冷启动两个例子）

**例子A：户外帐篷（三SKU）**

Mock SKU库：

| sku_id | 名称 | 价格 | 客观参数 |
|--------|------|------|----------|
| sku_tent_mgd | 牧高笛 零动155 | ¥259 | 速开3s、一室一厅、3-5人、PU2000mm、UPF50+、3.4kg |
| sku_tent_nhk | 挪客户外 ango 三人款 | ¥279 | 铝合金杆、四面开窗、3人、PU3000mm、2.9kg |
| sku_tent_dcl | 迪卡侬 Quechua 2秒速开 | ¥149 | 玻纤杆、两门两窗、2人、PU2000mm、3.3kg |
| sku_tent_cml | 骆驼 全自动液压帐篷（换品候选） | ¥199 | 液压自动杆、一室一厅、3-4人、PU3000mm、4.5kg |
| sku_tent_txz | 探险者 超轻速开2人（换品候选） | ¥129 | 速开、2人、PU1500mm、2.2kg |

换品候选两SKU（F44）供「修改对比对象」抽屉换入换出演示：各配3篇笔记（骆驼：183cm液压实测/158cm野餐/新手开箱；探险者：165cm轻量实测/170cm骑行/遛娃观点），换入后重跑报告——事实标签重算（探险者¥129变最低价、2.2kg变最轻）、人群重分群、无数据维度如实展示。

Mock笔记库（18篇=初始12篇+换品候选6篇，示例3篇）：

```json
[{
  "note_id": "note_t_001", "sku_id": "sku_tent_mgd",
  "type": "video_test", "is_purchased": true, "is_reported": false,
  "author_crowd": "158cm/45kg，公园野餐党",
  "text": "零动155我入的第二年了，3秒打开是真的爽。但我158站里面顶头，空间利用一般，170+的男朋友觉得刚好。下小雨完全扛得住。"
}, {
  "note_id": "note_t_002", "sku_id": "sku_tent_nhk",
  "type": "image_test", "is_purchased": true, "is_reported": false,
  "author_crowd": "露营新手，自驾",
  "text": "ango装拆确实比朋友的零动快，四面开窗透气绝了。缺点是高瘦型，底面积小，我们仨坐着挤。防水还没遇过大雨。"
}, {
  "note_id": "note_t_003", "sku_id": "sku_tent_dcl",
  "type": "unbox", "is_purchased": true, "is_reported": false,
  "author_crowd": "178cm，地铁通勤野餐",
  "text": "迪卡侬这款149真香，2秒速开名不虚传。玻纤杆轻但感觉长期用悬，一年用三五次足够了。地铁背去公园不累。"
}]
```

预期演示效果：维度行"空间"输出分群结论——零动155：170+说刚好、158说顶头（2条证据）；ango：3人坐挤（1条，样本不足标注）；"耐久"维度迪卡侬标"玻纤杆断裂风险（观点陈述，无实测证据）"。

**例子B：女装西装（三SKU，2026-09-22 已实现）**

Mock SKU库：

| sku_id | 名称 | 价格 | 客观参数（tags） |
|--------|------|------|----------|
| sku_suit_zz | 致知 oversize羊毛混纺西装 | ¥899 | oversize、羊毛混纺、四粒扣、中长款、165+友好 |
| sku_suit_mx | 茉寻 修身通勤西装 | ¥499 | 修身、涤纶混纺、单排一粒扣、常规长度、通勤 |
| sku_suit_prr | 品肉肉 小个子短款棉混纺西装 | ¥329 | 小码特调、棉混纺、两粒扣、短款、155友好 |

品类知识表7维（demo/src/data/knowledge-table.js，category='suit'）：版型与尺码/面料与垂感/显瘦效果/通勤适配/抗皱表现（主观）+ 价格/面料成分（客观，面料成分直读SKU tags）。

Mock笔记库（7篇=致知2+茉寻3+品肉肉2，示例3篇）：

```json
[{
  "note_id": "note_s_001", "sku_id": "sku_suit_zz",
  "type": "video_test", "is_purchased": true, "is_reported": false,
  "author_crowd": "166cm，通勤党",
  "text": "致知oversize西装165+实测。落肩设计我166穿刚好是松弛感。155的朋友试了会压身高显矮，小个子慎入。羊毛混纺垂感在线。"
}, {
  "note_id": "note_s_004", "sku_id": "sku_suit_mx",
  "type": "video_test", "is_purchased": false, "is_reported": true,
  "author_crowd": "品牌合作（报备商单）",
  "text": "茉寻西装通勤实测（品牌合作，已报备）。涤纶混纺回弹不错，褶抖一抖就散。一粒扣扣上通勤感强。"
}, {
  "note_id": "note_s_007", "sku_id": "sku_suit_mx",
  "type": "image_test", "is_purchased": true, "is_reported": false,
  "author_crowd": "通勤党",
  "text": "茉寻这4.7分老实说有点虚。涤纶混纺透气一般，中午热的时候有点闷。垂感不如羊毛挺括，胜在好打理机洗不变形。"
}]
```

预期演示效果：维度行"版型与尺码"输出155/165+核心分群——致知：165+松弛感/155压身高慎入；茉寻：修身卡胯155合身；品肉肉：小码特调155友好/短款卡胯165+不友好（各1条，样本不足标注）；"面料成分"客观行直读SKU标签（羊毛混纺/涤纶混纺/棉混纺，不经过LLM、无最低价类事实标签）；"显瘦效果"茉寻无证据 → 如实展示「无数据」；注：note_s_004为报备商单（is_reported: true），冲突场景演示降权逻辑。

**Demo数据流转**：维度选择完成 → 直接传该例子全部mock笔记进抽取层；追加维度点"+"→ 填入下一个预设维度，同样直接传mock笔记（初始与追加均无检索步骤，F37/F38）。**管线按组首SKU类目过滤知识表（F46）——帐篷组只喂帐篷9维+帐篷笔记，西装组只喂西装7维+西装笔记，跨类目维度不进报告。**帐篷例子18篇笔记覆盖四个教学场景——分群结论（158/170+/新手/通勤）、样本不足标注（单条证据的维度）、报备商单降权（note_t_011 is_reported=true）、修改对比对象换入换出重跑报告（F44）；西装例子7篇笔记覆盖四个教学场景——155/165+分群、报备商单降权（note_s_004 is_reported=true）、无数据如实展示（茉寻显瘦维度）、客观行标签直读（面料成分）。

### 4.3 数据模型 / 字段清单

**实体：品类知识表（CategoryDimension）**

| 字段名 | 类型 | 必填 | 说明 | 示例 | 约束 / 来源 |
|--------|------|------|------|------|-------------|
| id | string | 是 | 维度ID | dim_clothing_fit | 唯一 |
| category | enum | 是 | 品类 | clothing / skincare | v1仅两值 |
| name | string | 是 | 维度名 | 版型 | 展示用 |
| layer | enum | 是 | 层级 | objective / subjective | 客观/主观分组 |
| hot_rank | int | 是 | 热度排名（默认勾选依据） | 2 | 前3-5预勾（F32） |
| status | enum | 是 | 状态 | preset / candidate | candidate=自定义回写待审 |

**实体：证据元组（EvidenceTuple）**

| 字段名 | 类型 | 必填 | 说明 | 示例 | 约束 / 来源 |
|--------|------|------|------|------|-------------|
| id | string | 是 | 元组ID | et_001 | 唯一 |
| product_id | string | 是 | 商品ID | spu_123 | 组品解析产出 |
| dimension_id | string | 是 | 维度ID（含自定义维度） | dim_clothing_fit | |
| conclusion | string | 是 | 忠实转述结论 | 155-160cm穿M码偏大 | 抽取器产出，禁推断 |
| crowd_tag | string | 是 | 人群标签 | 155-160cm / 混油皮 / "通用" | 抽取器产出 |
| evidence_level | enum | 是 | 证据强度 | video_test / image_test / unbox / opinion | F18分级 |
| snippet | string | 是 | 原文片段（溯源） | "我158/45kg，M码袖长刚好但衣长偏长" | 抽取器产出 |
| note_id | string | 是 | 溯源笔记ID | note_456 | 点点检索返回 |
| is_purchased | bool | 否 | 已购标记 | true | 上游订单数据（F11） |
| is_reported | bool | 否 | 报备商单标记 | false | 蒲公英数据（F11） |
| created_at | timestamp | 是 | 抽取时间 | | 缓存时效依据 |

**实体：对比任务（CompareTask）**

| 字段名 | 类型 | 必填 | 说明 | 示例 | 约束 / 来源 |
|--------|------|------|------|------|-------------|
| id | string | 是 | 任务ID | ct_789 | 唯一 |
| user_id | string | 是 | 用户ID | u_001 | |
| entry | enum | 是 | 入口 | diandian_card / product_bibi | F25/F39（商品页入口=「比比」侧边栏） |
| product_ids | array | 是 | 对比商品（2-3件） | [spu_1,spu_2,spu_3] | F29上限 |
| dimension_ids | array | 是 | 所选维度（预设+自定义） | [dim_fit,"自定义:显白"] | F26用户选题 |
| status | enum | 是 | 状态 | building / done / timeout | |
| outcome | enum | 否 | 决策终局 | buy / no_buy / wait / none_good / null | 未收尾为null（F12） |
| forwarded | bool | 否 | 是否转发 | true | 裂变追踪 |

### 4.4 关键接口

| 接口 | 方法 | 入参 | 出参 | 说明 |
|------|------|------|------|------|
| /compare/intent | POST | 搜索query/商品页上下文 | 是否出多选预填+置信度 | 包装点点意图识别（F35预填依据） |
| /compare/products | GET | 关键词/链接 | 标准商品列表 | 换品时的同类候选搜索 |
| /compare/report | POST | product_ids+dimension_ids | 对比条目JSON（含客观层+AI层） | 主接口；缓存优先，miss则实时管线 |
| /compare/dimension/append | POST | task_id+新维度 | 增量维度行 | 追加维度（F26） |
| /compare/evidence | GET | tuple_id | 证据详情+笔记跳转参数 | 溯源面板 |
| /compare/outcome | POST | task_id+outcome | ok | 决策终局埋点（北极星） |

### 4.5 数据需求

真实上线的数据全景（demo阶段全部mock同签名预演，F36；接真实数据只换适配器）。其中**已购标记与报备商单库（#3/#4）是第三方产品拿不到的站内数据**——"认证的信任 vs 推测的信任"壁垒的数据落点。

**v1必需数据清单**：

| # | 数据集 | 来源系统 | 关键字段 | 管线用途 | 关键约束 / 口径 | demo状态 |
|---|--------|----------|----------|----------|----------------|----------|
| 1 | **笔记内容数据**（核心生产资料） | 站内笔记库 | note_id、正文文本、笔记类型（视频/图文）、发布时间、点赞/收藏数 | 抽取层唯一输入——结论/snippet/人群标签/证据强度全部源于笔记原文 | ①视频笔记需ASR字幕文本且**带时间戳**（支撑溯源"直达视频时间点"，§2.1）；图文笔记需OCR文本；②人群标签由抽取器从正文自述（身高/体重/肤质）识别，非上游结构化字段、非平台画像（§7）；③snippet必须是正文子串（§5溯源铁律） | 帐篷例子18篇（12初始+6换品候选）+西装例子7篇（字段与真实同构） |
| 2 | **笔记-SKU关联** | 商品挂载关系（挂载笔记）+ 点点检索匹配（未挂载笔记） | note_id × sku_id | 检索层把召回笔记归到具体SKU（→证据元组product_id） | 未挂载笔记按"商品名+别名"匹配进对比池——真实维度大量散落在未挂载笔记中，顺带解决挂载率低痛点 | mock笔记直接带sku_id字段 |
| 3 | **已购标记** | 订单系统（订单级） | 作者user_id × sku_id × 订单时间 | 证据加权：is_purchased=true的元组排序前置+「已购实测」展示标签 | 只用于加权与标签展示，不对外暴露订单信息；作为上游标记附加于笔记对象，不进入LLM输入（F11） | mock字段 is_purchased |
| 4 | **报备商单标记** | 蒲公英报备库 | note_id × 报备状态 × 品牌方 | 冲突降权：报备证据snips排序后置+溯源浮层标「报备商单」 | 只降权、不删除、不标"广告"字样——报告锚定商品质量，永不锚定笔记是否广告（F10） | mock字段 is_reported（note_t_011/note_s_004） |
| 5 | **SKU结构化数据** | 商品库（电商中台） | sku_id、名称、品牌、当前价、客观参数、类目 | 客观条件层直读（不经过LLM）+ 商品头卡/组品候选渲染 | 客观参数按品类知识表schema映射对齐（保证列间可比）；价格=站内挂载价（全网比价不在v1） | mock SKU库两组例子 |
| 6 | **点点意图识别与检索** | 点点（既有基建，F22/F34） | 意图识别结果 + 搜索召回列表 | 入口A意图预填判定；检索层"商品×维度词"召回笔记 | 召回质量直接决定证据覆盖——召回<3条的维度诚实标「样本不足」，禁通用知识填充（F30） | mock：query含对比/vs/怎么选→预填；检索步骤整体跳过（F37） |
| 7 | **品类知识表** | 自建资产（运营人工维护，F32） | §4.3 CategoryDimension全字段 | 维度选择页预设菜单；抽取的维度脊柱；#5 SKU参数映射schema | 冷启动女装/户外两套，3-9维/类目；自定义维度热度回写candidate状态，人工审核后转preset | 真实实现（demo/src/data/knowledge-table.js，两类目16维=帐篷9+西装7，带category字段） |
| 8 | **决策终局埋点** | 本功能自产 | §4.3 CompareTask（entry/outcome/forwarded） | 北极星与全漏斗指标度量（§6） | 终局四枚举（buy/no_buy/wait/none_good）平权记录，买和不买都是成功 | mock（toast演示） |

**跨数据集口径**：
1. **身份字段禁止LLM产出**：note_id/sku_id由抽取层从上游数据回填，防止模型伪造溯源（demo已按此实现）；
2. **上游标记与模型隔离**：已购/报备由数据侧附加（F11），不进系统提示词、不由模型推断——中立性不依赖模型自觉；
3. **覆盖假设**：单SKU单维度证据≥3条才出可信结论；热门SKU笔记池预期≥20篇，长尾SKU样本不足为常态——标注而非阻断（诚实性优先于覆盖率）。

**v2+数据需求**（对应§8范围外模块）：

| 数据 | 来源系统 | 解锁的v2能力 |
|------|----------|--------------|
| 行为链归因（同品类连看≥3篇/收藏聚集/反复品类搜索） | 行为数据 | 隐性对比意图识别→主动触发入口（v1仅显性query意图） |
| 站内评论 + 退货原因库 | 评论系统/售后系统 | 单品拔草报告差评透视；对比报告"货不对板"维度 |
| 供给端质量数据（SKU退货率/差评率） | 电商中台 | 客观层质量维度扩展；B端选品简报资产 |

---

## 5. 异常处理与兜底策略

| 异常场景 | 触发条件 | 系统行为 | 用户可见反馈 | 兜底 / 升级 |
|----------|----------|----------|--------------|-------------|
| **维度无证据** | 某商品某维度检索笔记数=0或抽取元组=0 | 该单元格显示"无数据"（F30铁律：禁用通用知识填充）；维度行整体无证据则不展示该行 | "该维度暂无笔记证据" | 正式版：提示相近维度（知识表同义词归并）；demo：预设数据已保证主要维度有证据 |
| **样本不足** | 某维度某商品证据数<3 | 正常陈列但显著标注"样本不足（N条）"，排序下沉 | 结论旁灰色标签 | 不升人工；自定义维度天然常处此态，标注即处理 |
| **结论冲突无法分群** | 冲突双方人群标签均为"通用"（无可分群依据） | 并列展示两条结论+各自证据数+证据强度，注明"证据存在分歧" | 两条结论并列，不做调和 | 高频冲突商品进入人工审核队列（正式版）；demo中note_s_004报备降权演示此逻辑 |
| **LLM抽取超时/失败** | 抽取层调用超时或返回异常 | 已完成的部分维度先行渲染（渐进展示）；失败维度标"生成中"重试一次 | 对应单元格"生成中…"→重试后仍失败则"该维度暂时无法生成" | 重试2次仍失败：隐藏该单元格+记录失败日志；>30%维度失败则整体降级为"笔记列表模式"（直接展示检索到的原始笔记卡片） |
| **LLM幻觉/越界抽取** | 抽取结论无法溯源到原文片段（snippet与conclusion不匹配） | 汇编层校验：snippet含conclusion关键词才通过，否则丢弃该元组 | 用户不可见（静默丢弃+计数） | 丢弃率>10%触发提示词review；溯源面板保证用户可自查（F17） |
| **自定义维度语义模糊** | 用户输入如"好用吗"（正式版） | 澄清反问一轮（§3.3）；拒绝第二次模糊输入则按字面检索并标注"按字面理解" | 反问chips："你关心持妆/遮瑕/肤感？" | demo无此路径（+号直接填预设维度，F38） |
| **组品不足2件** | 用户仅勾选1件就点"比一比" | 浮条按钮置灰+文案引导 | "再勾1件就可以比啦" | 长按换品/继续阅读不阻断 |
| **跨品类混选** | 购物车勾选了帐篷+西装（demo两例子混选） | 触发开比时**自动排除其他类目**（F46）：主导类目以当前浏览商品为准（市集页以首个勾选项为准），toast「已自动排除其他类目N件，比比仅支持同类目对比」；排除后同类<2件则拦截 | "已自动排除其他类目2件，比比仅支持同类目对比" | demo：`bb_cart` 可跨类目收集，勾选开比时按类目自动排除并提示（用户拍板，2026-09-22） |
| **生成等待过长** | 全管线>15秒 | loading态展示进度（"正在翻N篇笔记…"），支持后台生成+完成推送 | 进度反馈+可离开 | 正式版：预生成热门SKU×维度组合缓存；demo：25篇mock笔记秒级返回 |
| **品牌方投诉负面结论** | 品牌方对某结论申诉（正式版） | 结论旁挂"该结论存在争议"角标（不删），申诉走内容治理通道 | 角标+可查看申诉说明 | 全链路证据可溯源（snippet+note_id）是抗辩依据；F10锚点：只陈列笔记原文证据，平台不为结论背书 |

**demo特化**：①无检索/搜索，检索类异常（超时/空召回）不触发；②抽取与汇编层异常保留——这是demo要验证的真实管线；③跨品类混选、组品不足两条为demo实际可点的边界交互。

---

## 6. 成功指标与度量

**北极星：周人均决策终局数**（买/不买/再看看三选一完成数，F15——买和不买都是成功，中立性即价值）

| 层 | 指标 | 目标（首期参考） | 测量方式 |
|----|------|------------------|----------|
| 触发 | 泛搜结果多选勾选率（勾≥2件用户/看到多选UI用户） | ≥15% | 埋点 |
| 生成 | 对比报告生成完成率（生成任务完成/发起） | ≥90% | 埋点（含降级率） |
| 质量 | 抽取元组人工抽检准确率（结论忠实原文/抽检总数） | ≥90%（幻觉<10%） | 每日抽30份人工标注（§5幻觉校验丢弃率为线上代理） |
| 行为 | 报告完读率（滚动至收尾卡/生成完成） | ≥60% | 埋点 |
| **北极星** | **周人均决策终局数**；决策终局率（三选一完成/报告完读） | 终局率≥50% | 埋点 |
| 裂变 | 转发卡生成率、被转发卡回访率 | 生成≥10% | 埋点+分享链路 |
| 护栏 | 用户纠错率（"报告有错"点击/报告曝光） | <3% | 埋点 |
| 护栏 | 导购倾向检出（含推荐性表述的报告/抽检数） | 0（红线，F30） | 人工抽检 |
| 护栏 | 对比挂链转化率 | 只监测不作KPI（F15） | 埋点 |

**Kill标准**：D7复访<10% 或 终局率<30% → 痛点不成立或形态失效；完读率高但终局率低 → 改收尾交互而非弃方向。

## 7. 安全、合规与护栏

- **内容中立红线**（F10/F30）：系统提示词铁律禁推荐性表述+导购倾向检出0容错；报告永不判定笔记是否广告，报备标记只用于冲突降权
- **证据可溯源**：每条结论必须挂snippet+note_id，无溯源结论在汇编层被丢弃（§5）——既是信任机制也是品牌方投诉时的抗辩依据
- **用户输入防注入**：自定义维度输入过LLM指令安全过滤（维度语义解析层隔离，禁止把用户输入拼进系统提示词）
- **mock数据合规**：demo笔记为虚构内容但使用真实品牌名，演示外发场景需标注"演示数据"
- **个人数据**：人群标签来自笔记作者自述（身高/体重/肤质），非平台画像——展示时标注"来自作者分享"

## 8. 范围外 / 依赖与风险（轻量）

**范围外（v1不做）**：讨论层（价格公投/证词翻案）、单品拔草报告、真实检索与SKU发现（点点基建）、对比卡互动化、3C品类、人群筛选条（原§2.1维度行下方chips筛选，2026-09-21评审删除——分群结论已内嵌在单元格内，二次筛选徒增理解成本；人群差异靠分群陈列呈现，不靠筛选器）。

**依赖**：点点意图识别与检索API（demo阶段mock同签名预演）；LLM抽取/汇编服务；品类知识表冷启动人工维护（女装/户外两套）。

**主要风险**：①抽取质量不达标→评测迭代提示词+知识表；②用户把对比表当免费导购用完即走→北极星盯终局率而非使用量；③组织风险：被广告部门质疑→F10锚点（只砸劣质供给不砸内容生态）+淘宝问大家先例。

**上线节奏**：demo（全mock两例子）→ 内部评审 → 接点点真实API灰度（女装/户外单品类）→ 全品类复制。
